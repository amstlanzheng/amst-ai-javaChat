<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.amst.ai.mapper.CourseReservationMapper">

    <resultMap id="BaseResultMap" type="com.amst.ai.model.entity.CourseReservationEntity">
        <!--@Table course_reservation-->
                    <result property="id" column="id" jdbcType="INTEGER"/>
                    <result property="course" column="course" jdbcType="VARCHAR"/>
                    <result property="studentName" column="student_name" jdbcType="VARCHAR"/>
                    <result property="contactInfo" column="contact_info" jdbcType="VARCHAR"/>
                    <result property="school" column="school" jdbcType="VARCHAR"/>
                    <result property="remark" column="remark" jdbcType="VARCHAR"/>
            </resultMap>

    <sql id="Base_Column_List">
        id,course,student_name,contact_info,school,remark
    </sql>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into tq_mobile.course_reservation(course, student_name, contact_info, school, remark)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.course}, #{entity.studentName}, #{entity.contactInfo}, #{entity.school}, #{entity.remark})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into tq_mobile.course_reservation(course, student_name, contact_info, school, remark)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.course}, #{entity.studentName}, #{entity.contactInfo}, #{entity.school}, #{entity.remark})
        </foreach>
        on duplicate key update
         course = values(course) , student_name = values(student_name) , contact_info = values(contact_info) , school = values(school) , remark = values(remark)     </insert>

</mapper>

